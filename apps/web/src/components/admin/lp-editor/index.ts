/**
 * Wave 14b LP-ADMIN-UX — barrel export.
 *
 * Public surface (consumed by `apps/web/src/components/lp/admin-lp-form.tsx`):
 *   - `<SectionsTab>` — top-level Sections tab (drag-reorder + per-type forms)
 *   - `<HeroForm>`    — top-level Hero tab (featured image + scalar hero fields)
 *   Everything else is internal.
 */

export { SectionsTab } from './sections-tab';
export { HeroForm } from './hero-form';
